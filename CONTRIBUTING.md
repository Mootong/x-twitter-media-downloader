# 开发约定

## 分支与提交

- `main` 始终保持可加载、可验证。
- 功能分支使用 `feature/<名称>`。
- 修复分支使用 `fix/<名称>`。
- 提交信息使用 `类型: 简短说明`，例如 `feat: 增加下载任务进度`。

## 修改流程

1. 在 `docs/ROADMAP.md` 或 Issue 中描述目标和验收条件。
2. 创建功能分支。
3. 修改代码，同时更新相关文档和变更记录。
4. 执行 `npm run validate`。
5. 在 Chrome 中重新加载扩展并完成手工验证。
6. 执行 `npm run package` 生成发布包。

## 完成标准

- Manifest 与脚本通过静态验证。
- 普通图片和 MP4 下载未出现回归。
- 涉及 GraphQL 的修改包含脱敏响应样本或明确的手工验证记录。
- 新权限必须在 `docs/SECURITY.md` 中说明原因。
- 用户可见变化写入 `CHANGELOG.md`。
